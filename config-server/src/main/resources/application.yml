server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          search-paths: desenvolvimento, homologacao, producao, usuario
          uri: https://github.com/HarturAlcantaraDev/config-server
